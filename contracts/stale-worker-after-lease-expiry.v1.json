{
  "id": "pfc.stale-worker-after-lease-expiry",
  "version": "1.0.0",
  "family": "stale worker after lease expiry",
  "lifecycle": "candidate",
  "verification_level": "L0 CANDIDATE",
  "provenance": "Gate A family 3 mechanism mapping; synthetic execution is not public incident evidence",
  "prerequisites": ["time_bounded_lease", "ownership_expires", "work_outlives_lease", "reacquisition_after_expiry", "stale_worker_resumes", "durable_protected_effect", "ownership_token_observable", "protected_effect_observable"],
  "control_under_test": "protected-resource fencing token enforcement",
  "causal_sequence": ["worker A acquires token 1 and starts work", "worker A stalls past deterministic lease expiry", "worker B acquires newer token 2", "worker B commits protected effect", "worker A resumes and attempts protected write with stale token 1"],
  "forbidden_outcome": "older token commits protected effect after newer token becomes authoritative",
  "invariant": "an older fencing token must never commit a protected effect after a newer token becomes authoritative",
  "experiment": {"fault":"DELAY_WORKER_PAST_LEASE_EXPIRY","attempts":2,"concurrency":2},
  "proof_requirements": ["authoritative PostgreSQL lease and token history", "complete ordered worker and expiry trace", "authoritative protected-effect ledger", "durable stale-write attempt and rejection when fenced"],
  "safety_ceiling": 1
}
