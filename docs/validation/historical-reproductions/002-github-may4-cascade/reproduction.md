# Bounded historical mechanism reproduction

With an isolated local PostgreSQL URL in `FAILUREMESH_DATABASE_URL`, run `failuremesh historical github-may4-cascade match`, `plan`, `init`, `reset`, `run`, and `verify`. `run` requires empty #002 tables and an approved incident-specific level-1 plan. The CLI hashes the public source record; a changed digest blocks compilation.

The deterministic logical sequence initializes capacity 4; migration-M takes 2 at tick 1; normal production takes 2 at tick 2; all 4 are occupied and none available. At tick 3, pull, secondary, and dependent requests sharing `primary-db` enter waiting state. Logical decision tick 5 crosses their tick-4/5 deadlines; PostgreSQL stores TIMEOUT, TIMEOUT, and DEGRADED_TIMEOUT. Migration-M releases at tick 6, making 2 units available; a later pull request completes at tick 7. These ticks are **SYNTHETIC LOCAL MODEL PARAMETERS**, not wall-clock timing.

The verifier first checks a private structural comparison of exact, bounded state: workload intervals and occupancy arithmetic for every history tick, zero availability when all affected requests arrive, explicit shared graph edges, waiting/deadline/outcome rows from three service classes, migration release, post-release capacity and completion, complete trace and expected row counts. Its sole exported positive path re-reads PostgreSQL and rejects changed or missing authoritative rows. Trace labels alone cannot establish the cascade. `verify` captures the current DB state but still uses the same DB-backed verifier. A clean reset is required before repeating `run`.
